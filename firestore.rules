rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {

    match /users/{userId} {
      allow read, write: if isOwner(userId);
    }

    match /videoFragments/{videoId} {
      allow get: if true;
      allow list: if false; // Explicitly disallow listing from the main collection
      allow write: if isAdmin();
    }

    match /publicVideoFragments/{videoId} {
      allow get, list: if true; // Allow public read access
      allow write: if isAdmin(); // Only admins can write to it
    }

    match /pendingVideoFragments/{videoId} {
      allow read, update, delete: if isAdmin();
      allow create: if isSignedIn() && request.resource.data.uploaderId == request.auth.uid;
      allow list: if isAdmin();
    }

    match /roles_admin/{userId} {
      allow get: if isOwner(userId);
      allow list: if false; 
      allow create, delete: if isAdmin();
      allow update: if false;
    }

    function isSignedIn() {
      return request.auth != null;
    }

    function isOwner(userId) {
      return isSignedIn() && request.auth.uid == userId;
    }

    function isAdmin() {
      return isSignedIn() && exists(/databases/$(database)/documents/roles_admin/$(request.auth.uid));
    }
  }
}
